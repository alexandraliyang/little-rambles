# PRD: v0.12 — Edit memories
Target: App 0.12.0 · [ASSUMPTION, ADR-0009] · The "next cycle" item from v0.11's NOT-building list.
## Done means
- ✏️ on every memory (visits + journal) → editor: note/journal text; place (visits); add photos (cap 6); remove photos (✕ on thumbnail); change rating (visits — flows into engine as corrected child-response record); Save.
- Delete with two-tap confirm ("Delete" → "Tap again to delete forever"); photos storage cleaned up on delete and on photo-empty save.
## NOT building: edit history/undo; editing timestamps; bulk operations.
